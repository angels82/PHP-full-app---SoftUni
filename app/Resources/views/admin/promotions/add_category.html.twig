{{ form_start(add_form) }}
    {{ form_row(add_form.promotion) }}
    {{ form_row(add_form.category) }}

<button class="btn btn-success" formnovalidate>Add <span class="glyphicon glyphicon-plus-sign"></span></button>

{{ form_end(add_form) }}