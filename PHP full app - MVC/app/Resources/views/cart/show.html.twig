<ol>
    {% if arr != null %}

{% set sum = 0 %}
        {% for key, value in arr %}

            {{ key }}
            {{ value.count }}
            {{ value.price }}
            {{ value.id }}
            <a href="{{ path('remove_item', {'itemProduct':value.id,'itemCart':cart_data.id}) }}">Remove</a>
            {% set sum = sum + value.count*value.price %}
<br>

        {% endfor %}

{{ sum }}

    {% else %}
        No products found
    {% endif %}
</ol>

